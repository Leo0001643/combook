<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cambook.dao.mapper.CbOrderItemMapper">

    <!-- 查询指定订单下所有未删除的服务项 -->
    <select id="selectActiveByOrderId" resultType="com.cambook.dao.entity.CbOrderItem">
        SELECT id, order_id, service_item_id, service_name, service_duration,
               unit_price, qty, svc_status, start_time, end_time, remark,
               deleted, create_time, update_time
        FROM cb_order_item
        WHERE order_id = #{orderId}
          AND deleted = 0
        ORDER BY id ASC
    </select>

    <!-- 按多个订单 ID 批量加载服务项（避免 N+1）-->
    <select id="selectByOrderIds" resultType="com.cambook.dao.entity.CbOrderItem">
        SELECT id, order_id, service_item_id, service_name, service_duration,
               unit_price, qty, svc_status, start_time, end_time, remark,
               deleted, create_time, update_time
        FROM cb_order_item
        WHERE order_id IN
        <foreach collection="orderIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
          AND deleted = 0
        ORDER BY order_id ASC, id ASC
    </select>

</mapper>
