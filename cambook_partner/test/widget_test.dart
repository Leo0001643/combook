import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder test — replace with integration tests after backend is ready
    expect(true, isTrue);
  });
}
